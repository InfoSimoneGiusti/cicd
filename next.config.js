/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    loader: 'akamai',
    path: '',
  },
    basePath: '/repo',
    assetPrefix: '/repo/',
    basePath: '/repo',
    assetPrefix: '/repo/'
}

module.exports = nextConfig
